let arr = new Array (1000);

function ArrayArr(i) {
    for (i = 0; i <= 1000; i++) {
    return arr[i];
  }
}

ArrayArr(999);
console.log(ArrayArr());

//Вопрос: не смогла прописать условие, что если индекс больше 1000, чтобы выдавало сообщение 'no value'. По логике тогда будет undefined?